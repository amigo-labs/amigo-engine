name: Nightly

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

concurrency:
  group: nightly
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: amigo

jobs:
  build:
    name: Nightly (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: nightly-${{ matrix.target }}

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        uses: ./.github/actions/install-system-deps

      - name: Build release binaries
        run: cargo build --release --target ${{ matrix.target }} -p amigo_cli -p amigo_artgen -p amigo_audiogen

      # Keep the archive contents in sync with release.yml — the MCP configs
      # reference amigo-artgen and amigo-audiogen on PATH.
      - name: Package (Unix)
        if: matrix.archive == 'tar.gz'
        run: |
          STAGING="amigo-nightly-${{ matrix.target }}"
          mkdir -p "$STAGING"
          cp "target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}" "$STAGING/"
          cp "target/${{ matrix.target }}/release/amigo-artgen" "$STAGING/"
          cp "target/${{ matrix.target }}/release/amigo-audiogen" "$STAGING/"
          tar czf "${STAGING}.tar.gz" "$STAGING"
          echo "ASSET=${STAGING}.tar.gz" >> $GITHUB_ENV

      - name: Package (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $staging = "amigo-nightly-${{ matrix.target }}"
          New-Item -ItemType Directory -Path $staging -Force
          Copy-Item "target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}.exe" "$staging/"
          Copy-Item "target/${{ matrix.target }}/release/amigo-artgen.exe" "$staging/"
          Copy-Item "target/${{ matrix.target }}/release/amigo-audiogen.exe" "$staging/"
          Compress-Archive -Path "$staging/*" -DestinationPath "$staging.zip"
          echo "ASSET=$staging.zip" >> $env:GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: amigo-nightly-${{ matrix.target }}
          path: ${{ env.ASSET }}
          retention-days: 30
