[package]
name = "amigo_mcp"
version = "0.1.0"
edition = "2021"
description = "MCP server that bridges Claude Code to the Amigo Engine API"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
